/**
 * @file - prod_molecule.js
 */

(function ($) {

  Drupal.atomizer.producers.moleculeC = function (_viewer) {
    let viewer = _viewer;

    const onAtomAddButton = (event) => {

    };

    const onAtomDeleteButton = (event) => {

    };

    const moleculeLoaded = (molecule) => {

    };

    return {
      moleculeLoaded: moleculeLoaded,
    };
  };

})(jQuery);
